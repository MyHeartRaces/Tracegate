# Tracegate 3.1.31

- Adds a localhost-only HTTP probe inbound routed exclusively through the
  Reality fallback.
- Uses a real HTTPS request through that inbound for Reality health metrics,
  avoiding false negatives from Xray Observatory while still validating full
  egress.
- Includes the runtime hardening and native backup fixes from 3.1.29-3.1.30.
