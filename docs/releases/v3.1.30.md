# Tracegate 3.1.30

- Includes the Telemt alerting, private runtime permissions, interserver probe,
  and TCP loss-resilience changes from 3.1.29.
- Fixes the native PostgreSQL backup guard so `pipefail` cannot misclassify a
  valid custom-format archive as an empty TOC during deployment.
