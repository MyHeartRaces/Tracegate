#!/usr/bin/env bash
set -euo pipefail

cat >&2 <<'EOF'
Tracegate production deployment automation is intentionally not shipped from
the public repository.

Use the private operator repository for live promotion workflows. This file is a
public decoy kept only so accidental public-repo execution fails closed.
EOF

exit 2
